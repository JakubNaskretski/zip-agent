# Never commit the source ZIPs being analyzed
*.zip
*AgentDefinition*/
analysis-source/
samples/

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
build/
dist/
.pytest_cache/

# local-only working notes
*.local.md
obsidian-vault/

# private/ — local-only, never tracked: design plans, analysis notes, todos,
# example/sample data, and anything not strictly part of the shippable agent.
# This is the distinguishable home for non-agent material (see README).
/private/

# internal agent instructions / rules — keep local, never track (see ~/.claude/CLAUDE.md)
CLAUDE.md
CONTRACT.md
